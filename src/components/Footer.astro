---
export interface Props {
  slug?: string;
}
const {slug} = Astro.props as Props;

import LogoMark from './Header/LogoMark.astro';
import NavButton from './Header/NavButton.astro';
---

<>
  <footer>© 2022 DBGMODE Media, LLC.</footer>
</>

<style>
  $font-size: 2.25rem;
  $logo-width: 2.1--bgsq;

  footer {
    @mixin hrainbow 1--bgsq, 6.8 * $font-size, 100vw, border;

    color: rgb(187, 145, 117);

    display: flex;
    flex-direction: row-reverse;
    align-items: center;
    justify-items: start;

    height: calc(1--bgsq + 2--bgl);

    font-size: 1 rem;
    line-height: 1.1;

    padding-left: 1ex;

    border-style: dashed;
    border-top-width: 1--bgl;
    border-image-slice: 100% 0%;
    border-image-outset: 0 0 2--bgl 0;
  }
</style>
